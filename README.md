<div align="center">

</div>

# v-safe ai demo

This contains everything you need to run the V-safe AI demo locally.

## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`

The AI assistant is a local demo interaction. It does not require an API key or a backend service.
