# Assets

Use this folder for visual files that belong to the app UI.

- `logos/`: brand marks, V-safe logo variants, partner logos
- `icons/`: custom icon files that are not available in lucide-react
- `ui/`: button artwork, badges, backgrounds, and reusable interface images

Prefer importing assets from code instead of hotlinking external image URLs.
